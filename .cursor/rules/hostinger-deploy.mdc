---
description: Push changes to GitHub after completing work to trigger Hostinger redeploy
alwaysApply: true
---

# Hostinger Auto-Deploy

This project is deployed on Hostinger from `main` on GitHub (`MeekaaeelMartin/ai-fitness-coach`). Hostinger redeploys automatically when GitHub is linked.

After completing requested code changes:

1. Run `git status` and `git diff` to review changes
2. Stage and commit with a clear message describing the change
3. Push to `origin main` with `git push origin main`
4. Confirm the push succeeded so Hostinger starts a new deploy

Do not skip the push unless the user explicitly asks not to deploy.

Never commit secrets (`.env`, API keys, tokens).
